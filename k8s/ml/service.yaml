apiVersion: v1
kind: Service
metadata:
  name: ml
  namespace: saqbol
  labels:
    app: ml
spec:
  selector:
    app: ml
  ports:
    - name: grpc
      port: 9090
      targetPort: 9090
    - name: http
      port: 8000
      targetPort: 8000
